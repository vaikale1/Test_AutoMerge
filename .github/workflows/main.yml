name: Main Workflow

on:
  pull_request_target:
    types:
      - labeled
      - unlabeled
      - synchronize
      - opened
      - edited
      - ready_for_review
      - reopened
      - unlocked
  pull_request_review:
    types:
      - submitted
  check_suite:
    types:
      - completed
  status: {}

jobs:
  jobs:
  trigger_build:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Build Workflow via API
        run: |
          curl -X POST \
          -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/vaikale1/Test_AutoMerge/actions/workflows/build.yml/dispatches \
          -d '{"ref":"refs/heads/vaikale-patch-3"}'

  trigger_automerge:
    needs: trigger_build  # This ensures the next job runs only after the previous one completes
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Automerge Workflow using GitHub API
        run: |
          curl -X POST \
          -H "Authorization: token ${{ secrets.PERSONAL_ACCESS_TOKEN }}" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/${{ github.repository }}/actions/workflows/automerge.yml/dispatches \
          -d '{"ref":"refs/heads/${{ github.event.pull_request.head.ref }}"}'
